apiVersion: v1
kind: Service
metadata:
  name: web-stable
  namespace: mini-platform
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: web-canary
  namespace: mini-platform
spec:
  selector:
    app: web
  ports:
    - name: http
      port: 80
      targetPort: 80

